#include <stdio.h>

int faktorial (int n) {
	if (n == 0 || n == 1)
	return 1;
	
	else 
	return n * faktorial (n - 1);
}

int main() {
	int input;
	scanf("%d",&input);
	printf("%d! = %d \n",input,faktorial(input));
	
	return 0;
}
